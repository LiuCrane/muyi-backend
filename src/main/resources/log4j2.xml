<?xml version="1.0" encoding="UTF-8"?>
<Configuration monitorInterval="3600" shutdownHook="disable">

    <properties>
        <property name="LOG_HOME">./logs</property>
        <property name="LOG_FILE_SIZE">10 MB</property>
        <property name="SERVER_NAME">shili-api</property>
        <property name="OUTPUT_LOG_LEVEL">DEBUG</property>
        <Property name="LOG_PATTERN">[%clr{%X{userId}}{magenta} %clr{%X{username}}{magenta}] [%clr{${sys:PID}}{magenta}] %d{yyyy-MM-dd HH:mm:ss.SSS} [%p] [%t] %class.%M(%L) | %m%n</Property>
    </properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT" follow="true" >
            <PatternLayout>
                <Pattern>${sys:LOG_PATTERN}</Pattern>
            </PatternLayout>
        </Console>

        <RollingRandomAccessFile name="RandomAccessFile"
                     fileName="${LOG_HOME}/${SERVER_NAME}.app.log"
                     filePattern="${LOG_HOME}/${SERVER_NAME}.$${date:yyyy-MM}/app-%d{MM-dd-yyyy}-%i.log"
                     immediateFlush="false" append="false">
            <PatternLayout>
                <Pattern>${sys:LOG_PATTERN}</Pattern>
            </PatternLayout>

            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="${LOG_FILE_SIZE}"/>
            </Policies>
            <DefaultRolloverStrategy fileIndex="max" max="20"/>
        </RollingRandomAccessFile>

        <RollingRandomAccessFile name="RandomAccessFileForError"
                     fileName="${LOG_HOME}/${SERVER_NAME}.error.log"
                     filePattern="${LOG_HOME}/${SERVER_NAME}.$${date:yyyy-MM}/error-%d{MM-dd-yyyy}-%i.log"
                     append="false">
            <PatternLayout>
                <Pattern>${sys:LOG_PATTERN}</Pattern>
            </PatternLayout>

            <Policies>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
                <SizeBasedTriggeringPolicy size="${LOG_FILE_SIZE}"/>
            </Policies>
            <DefaultRolloverStrategy fileIndex="max" max="20"/>
            <Filters>
                <ThresholdFilter level="error" onMatch="ACCEPT" onMismatch="DENY"/>
            </Filters>
        </RollingRandomAccessFile>
    </Appenders>

    <Loggers>

        <!-- 定义单个类日志记录级别 -->
        <logger name="org.mybatis" level="info" />
        <logger name="org.springframework" level="info" />
        <logger name="com.zaxxer.hikari" level="info"/>

        <AsyncLogger name="com.mysl"  level="info" includeLocation="true" additivity="false">
            <AppenderRef ref="RandomAccessFile"/>
            <AppenderRef ref="RandomAccessFileForError"/>
        </AsyncLogger>

        <!-- 定义除单个类外所有日志记录级别 -->
        <Root level="info" includeLocation="true">
            <AppenderRef ref="RandomAccessFile"/>
            <AppenderRef ref="RandomAccessFileForError"/>
        </Root>
    </Loggers>

</Configuration>














