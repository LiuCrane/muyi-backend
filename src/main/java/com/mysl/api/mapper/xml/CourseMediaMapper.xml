<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mysl.api.mapper.CourseMediaMapper">
    <select id="findMediaIds" resultType="java.lang.Long">
        SELECT cm.media_id FROM course_media cm WHERE cm.course_id = #{course_id}
    </select>
    <delete id="deleteByCourseId">
        DELETE FROM course_media WHERE course_id = #{course_id}
    </delete>
    <delete id="deleteOne">
        DELETE FROM course_media WHERE course_id = #{course_id} AND media_id = #{media_id}
    </delete>
</mapper>
